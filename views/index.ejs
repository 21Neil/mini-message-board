<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <main>
    <ul>
      <% messages.forEach(message => { %>
        <li>
          <div>
            <p><%= message.username %></p>
            <p><%= message.text %></p>
            <p><%= message.added %></p>
          </div>
          <button><a href="/detail/<%= message.id %>">Open</a></button>
        </li>
      <% }) %>
    </ul>
    <button><a href="/new">Post message</a></button>
  </main>
</body>
</html>